# Optional. Required only when running the AI server locally.
# Used for define, explain, and summarize actions.
GEMINI_API_KEY=""

# Optional. Used by the desktop/frontend app to call a hosted AI API.
# Leave empty if AI is disabled or unavailable.
VITE_AI_API_BASE_URL=""
